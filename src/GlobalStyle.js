import { createGlobalStyle } from 'styled-components'

const GlobalStyle = createGlobalStyle`
  *{
       margin: 0;
       padding: 0;
       box-sizing: border-box;

       /* Solid Color Pallet 1 */
      --header-text: #cad2c5ff;
      --dark-sea-green: #84a98cff;
      --hookers-green: #52796fff;
      --dark-slate-gray: #354f52ff;
      --charcoal: #2f3e46ff;

      /* Solid Color Pallet 2 */
      --midnight-green-eagle-green: #003844ff;
      --cerulean-frost: #6096baff;
      --almond: #eae0d5ff;
      --morning-blue: #839791ff;
      --middle-yellow-red: #e1b07eff;

      /* Solid Color Pallet 3 */

      }
  `

export default GlobalStyle
